#include <stdio.h>
#include <locale.h>

int main() {
	setlocale(LC_ALL, "");
	int i, numero, divisao = 0;
	
	for(i = 1; i <= 5; i++) {
		printf("Digite o %iº número: ", i);
		scanf("%i",&numero);
		
		divisao = divisao + numero;
	}
	
	printf(" divisao: %i \n", divisao);
	

}
